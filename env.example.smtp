# SMTP邮件配置示例
# 将此文件复制为 .env.local 并填入实际配置

# ===========================================
# 通用SMTP配置（推荐使用）
# ===========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com
SMTP_FROM_NAME=PCB Manufacturing

# ===========================================
# 常见邮件服务商配置示例
# ===========================================

# Gmail
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# QQ邮箱
# SMTP_HOST=smtp.qq.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=your-email@qq.com
# SMTP_PASS=your-authorization-code

# 163邮箱
# SMTP_HOST=smtp.163.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=your-email@163.com
# SMTP_PASS=your-authorization-code

# 腾讯企业邮箱
# SMTP_HOST=smtp.exmail.qq.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=your-email@yourcompany.com
# SMTP_PASS=your-password

# 阿里云邮箱
# SMTP_HOST=smtp.mxhichina.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=your-email@yourcompany.com
# SMTP_PASS=your-password

# Outlook/Hotmail
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@outlook.com
# SMTP_PASS=your-password

# ===========================================
# 兼容性配置（向后兼容）
# ===========================================
# 如果已有QQ邮箱配置，系统会自动使用
# QQ_EMAIL_USER=your-email@qq.com
# QQ_EMAIL_AUTH_CODE=your-authorization-code

# ===========================================
# 配置说明
# ===========================================
# SMTP_HOST: SMTP服务器地址
# SMTP_PORT: SMTP端口（通常587或465）
# SMTP_SECURE: 是否使用SSL/TLS（465端口通常为true，587端口通常为false）
# SMTP_USER: 登录用户名（通常是邮箱地址）
# SMTP_PASS: 登录密码（Gmail等需要使用应用专用密码）
# SMTP_FROM: 发件人邮箱（可与SMTP_USER不同）
# SMTP_FROM_NAME: 发件人名称 